generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db" // Para SQL em memória, usaremos :memory:
}

model User {
  id        Int      @id @default(autoincrement())
  email     String   @unique
  password  String
  name      String
  isAdmin   Boolean  @default(false)
  posts     Post[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model State {
  id        Int      @id @default(autoincrement())
  name      String   @unique
  uf        String   @unique 
  cities    City[]
  posts     Post[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model City {
  id        Int      @id @default(autoincrement())
  name      String
  state     State    @relation(fields: [stateId], references: [id])
  stateId   Int
  neighborhoods String
  posts     Post[]
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@unique([name, stateId])
}

enum FloodSeverity {
  LOW
  MEDIUM
  HIGH
}

model Post {
  id           Int          @id @default(autoincrement())
  title        String
  description  String
  severity     FloodSeverity
  author       User         @relation(fields: [authorId], references: [id])
  authorId     Int
  state        State        @relation(fields: [stateId], references: [id])
  stateId      Int
  city         City         @relation(fields: [cityId], references: [id])
  cityId       Int
  neighborhood String
  createdAt    DateTime     @default(now())
  updatedAt    DateTime     @updatedAt
}